{
  "name": "ensight-creatives",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve",
    "build": "NODE_ENV=production eleventy && npm run css:build",
    "css:build": "postcss src/styles/tailwind.css -o _site/assets/app.css",
    "clean": "rm -rf _site .cache"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.12"
  }
}